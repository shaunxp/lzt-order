<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd" >
<mapper namespace="com.lzt.order.dao.OrderDao">
    <insert id="insert" parameterType="com.lzt.order.domain.Order" useGeneratedKeys="true" keyProperty="id">
        insert into wk_test (order_id
        )
        values (#{orderId,jdbcType=VARCHAR}
        )
    </insert>
</mapper>